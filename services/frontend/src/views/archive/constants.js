const ASC = 'ASC';
const DESC = 'DESC';

export const DEFAULT_SORT_ORDER = {
    lastModifiedAt: DESC,
    createdAt: DESC,
    publishedAt: DESC,
    title: ASC,
    lastEditStep: DESC,
    authorId: ASC,
    type: ASC
};
